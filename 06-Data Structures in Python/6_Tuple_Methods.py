t = (1,2,3,1,3,2,4,6,5,7,6,5,4,2)

# we can count an element how many times exist in the tuple, using count() method
print(t.count(2)) # 2 exsit in that tuple for 3 times


# we can get the first index number of an element, using index() method.
print(t.index(4))